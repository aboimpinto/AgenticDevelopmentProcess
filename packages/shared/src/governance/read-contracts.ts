// FEAT-068 V1 governance read transport. These are detached inspection DTOs;
// they carry no actor, authority, raw evidence, store path, or action request.
/** A metric count is validated at the producer boundary before transport. */
export type GovernanceNonNegativeIntegerV1 = number & { readonly __governanceNonNegativeIntegerV1: unique symbol };
export interface GovernanceCountV1 { readonly key: string; readonly count: GovernanceNonNegativeIntegerV1; }
export type GovernanceManifestResultV1 = "APPROVED" | "NEEDS_CHANGES" | "BLOCKED";
export type GovernanceFindingDispositionV1 = "IN_SCOPE_BLOCKER" | "SCOPE_EXPANSION" | "ARCHITECTURE_DEBT" | "OBSERVATION";
export type GovernanceFindingSeverityV1 = "blocker" | "required" | "note" | "info";
export type GovernanceDebtActionV1 = "ACCEPT_RISK" | "CLOSE" | "CONFIRM" | "DEFER" | "MERGE" | "PLAN_LINK" | "REASSIGN" | "REJECT" | "SUPERSEDE";
export type GovernanceReplanActionV1 = "APPROVE_REPLAN" | "REJECT_REPLAN";
export type GovernanceReplanStateV1 = "NORMAL_REMEDIATION" | "REMEDIATION_REPLAN_REQUIRED" | "REPLAN_PENDING_APPROVAL" | "REPLAN_APPROVED" | "REPLAN_REJECTED" | "BOUNDED_REMEDIATION_DISPATCHED" | "REVIEW_PENDING";
export type GovernanceScopeActionV1 = "ACCEPT_SCOPE_EXPANSION" | "REJECT_SCOPE_EXPANSION";
export interface GovernanceRemediationV1 {
  readonly reviewRunId: string; readonly featureId: string; readonly phaseNumber: number; readonly reviewGateId: string;
  readonly manifestHash: string; readonly manifestResult: GovernanceManifestResultV1; readonly ruleSnapshotHash: string; readonly createdAt: string;
  readonly gate: Readonly<{ gateState: "APPROVED" | "REJECTED" | "BLOCKED" | "PENDING"; reasonCode: string; basisManifestHash: string; cycleId: string | null; decidedAt: string }>;
  readonly cycleState: string;
  readonly findings: readonly Readonly<{ findingId: string; findingObservationId: string; disposition: GovernanceFindingDispositionV1; severity: GovernanceFindingSeverityV1; defectClass: string; summary: string; scopeDecisionTarget: Readonly<{ aggregateId: string; expectedVersion: number }> | null }>[];
  readonly receipts: readonly Readonly<{ findingId: string; subjectKind: "remediation_item" | "test"; subjectId: string; outcome: "VERIFIED" | "FAILED" | "NOT_VERIFIABLE" | "PASSED" | "NOT_RUN" }>[];
}
export interface GovernanceReplanV1 {
  readonly aggregateId: string; readonly featureId: string; readonly phaseNumber: number; readonly reviewGateId: string; readonly defectClass: string; readonly state: GovernanceReplanStateV1; readonly eventVersion: number;
  readonly recurrence: Readonly<{ postFixManifestations: number; acceptedScopeExpansions: number }>;
  readonly currentRequest: Readonly<{ requestId: string; planHash: string; planVersion: number; requestedAt: string }> | null;
  readonly scopeExpansionDecisions: readonly Readonly<{ decisionId: string; findingObservationId: string; outcome: "ACCEPT" | "REJECT"; actorId: string; authorizedRole: "FEATURE_OWNER"; reason: string; expectedVersion: number; resultingVersion: number; decidedAt: string }>[];
  readonly replanDecisions: readonly Readonly<{ decisionId: string; requestId: string; planHash: string; planVersion: number; outcome: "APPROVE" | "REJECT"; actorId: string; authorizedRole: "ARCHITECTURE_STEWARD"; reason: string; expectedVersion: number; resultingVersion: number; decidedAt: string }>[];
  readonly dispatch: Readonly<{ outcome: "STARTED" | "START_FAILED"; workflowRunId: string; attemptedAt: string }> | null;
  readonly summary: Readonly<{ observations: number; requests: number; decisions: number; dispatchAttempts: number; reviewAssessments: number }>;
  readonly availableActions: readonly GovernanceReplanActionV1[];
}
export interface GovernanceArchitectureDebtV1 {
  readonly recordId: string; readonly state: "PENDING_TRIAGE" | "CONFIRMED" | "DEFERRED" | "ACCEPTED_RISK" | "PLANNED" | "CLOSED" | "REJECTED" | "MERGED" | "SUPERSEDED"; readonly eventVersion: number; readonly ownerId: string; readonly priority: "P0" | "P1" | "P2" | "P3"; readonly prioritySource: "AUTO_PENDING_DEFAULT" | "STEWARD_CONFIRMED";
  readonly rule: Readonly<{ ruleId: string; ruleVersion: string; category: string; sourceReference: string }>; readonly architecturalBoundary: string; readonly rationale: string; readonly risk: string;
  readonly locations: readonly Readonly<{ locationId: string; relativePath: string; symbol?: string; endpoint?: string; ruleTags: readonly string[] }>[];
  readonly futureTouchTrigger: Readonly<{ triggerId: string; name: string; paths: readonly string[]; symbols: readonly string[]; ruleTags: readonly string[] }>;
  readonly discovery: Readonly<{ featureId: string; phaseNumber: number; reviewGateId: string; findingId: string }>;
  readonly duplicateOfRecordId?: string; readonly supersededByRecordId?: string;
  readonly futureTouchDecisions: readonly Readonly<{ decisionId: string; featureId: string; touchPlanHash: string; recordVersion: number; selectorIds: readonly string[]; kind: "REMEDIATE" | "PREREQUISITE" | "WAIVER" | "NON_INTERACTION"; actorId: string; authorizedRole: "ARCHITECTURE_STEWARD"; reason: string; occurredAt: string }> [];
  readonly availableActions: readonly GovernanceDebtActionV1[];
}
export interface GovernanceQueueItemV1 {
  readonly itemId: string; readonly itemKind: "REMEDIATION" | "REPLAN" | "ARCHITECTURE_DEBT"; readonly targetId: string; readonly featureId: string; readonly state: string; readonly currentVersion: number | null; readonly requiresAction: boolean; readonly urgency: "SCOPE_EXPANSION" | "P0" | "REPLAN_APPROVAL" | "REPLAN_REQUIRED" | "P1" | "P2" | "P3" | "INFORMATIONAL"; readonly summaryCode: string; readonly availableActions: readonly (GovernanceDebtActionV1 | GovernanceReplanActionV1 | GovernanceScopeActionV1)[];
}
export interface GovernanceMetricsV1 {
  readonly reviewResults: readonly GovernanceCountV1[]; readonly gateStates: readonly GovernanceCountV1[]; readonly cycleStates: readonly GovernanceCountV1[]; readonly findingDispositions: readonly GovernanceCountV1[]; readonly ruleReferences: readonly GovernanceCountV1[]; readonly recoveryStopReasons: readonly GovernanceCountV1[]; readonly replanStates: readonly GovernanceCountV1[]; readonly debtStates: readonly GovernanceCountV1[]; readonly debtPriorities: readonly GovernanceCountV1[]; readonly scopeDecisionOutcomes: readonly GovernanceCountV1[]; readonly replanDecisionOutcomes: readonly GovernanceCountV1[]; readonly futureTouchDecisionKinds: readonly GovernanceCountV1[]; readonly dispatchOutcomes: readonly GovernanceCountV1[]; readonly shadowOutcomes: readonly GovernanceCountV1[]; readonly pilotOutcomes: readonly GovernanceCountV1[];
  readonly reviewRuns: GovernanceNonNegativeIntegerV1; readonly openRemediationCycles: GovernanceNonNegativeIntegerV1; readonly replanAggregates: GovernanceNonNegativeIntegerV1; readonly architectureDebtRecords: GovernanceNonNegativeIntegerV1; readonly actionableQueueItems: GovernanceNonNegativeIntegerV1; readonly postFixManifestations: GovernanceNonNegativeIntegerV1; readonly acceptedScopeExpansions: GovernanceNonNegativeIntegerV1;
}
export interface GovernanceParityStatusV1 {
  readonly receiptId: string; readonly projectionSchema: "hepha-governance-parity/v1"; readonly sourceVersionHash: string; readonly authoritativeHash: string; readonly dashboardHash: string; readonly result: "MATCH" | "MISMATCH"; readonly differenceCategories: readonly ("SCHEMA" | "PROJECT" | "SOURCE_VERSION" | "REPLAN" | "DEBT" | "QUEUE" | "METRICS")[]; readonly comparedAt: string; readonly validUntil: string;
}
export interface GovernanceMigrationStatusV1 {
  readonly auditId: string; readonly schemaArea: "GOVERNANCE_ROLLOUT"; readonly fromVersion: number; readonly toVersion: number; readonly outcome: "APPLIED" | "ALREADY_CURRENT" | "FAILED"; readonly completedAt: string; readonly readBackHash: string | null;
}
export interface GovernancePilotStatusV1 {
  readonly pilotId: string; readonly featureId: string; readonly phaseContractId: string; readonly taskId: string; readonly contractVersion: number; readonly pilotConfigHash: string; readonly approvalReceiptId: string; readonly approvedAt: string; readonly expiresAt: string; readonly lastOutcome: string;
}
export interface GovernanceShadowRolloutV1 {
  readonly mode: "DISABLED" | "ACTIVE" | "NEEDS_HUMAN"; readonly eventVersion: number; readonly parity: GovernanceParityStatusV1 | null; readonly migration: GovernanceMigrationStatusV1 | null; readonly pilot: GovernancePilotStatusV1 | null;
}
export interface GovernanceDashboardReadV1 {
  readonly schemaVersion: "hepha-governance-dashboard/v1"; readonly projectId: string; readonly remediations: readonly GovernanceRemediationV1[]; readonly replans: readonly GovernanceReplanV1[]; readonly architectureDebt: readonly GovernanceArchitectureDebtV1[]; readonly queue: readonly GovernanceQueueItemV1[]; readonly metrics: GovernanceMetricsV1; readonly rollout: GovernanceShadowRolloutV1;
}
export type GovernanceReadRefusalCode =
  | "INVALID_REQUEST"
  | "PROJECT_NOT_FOUND"
  | "GOVERNANCE_STATE_CONFLICT"
  | "GOVERNANCE_STORE_UNAVAILABLE"
  | "UNSAFE_GOVERNANCE_PROJECTION";
export type GovernanceReadResultV1 =
  | Readonly<{ kind: "governance_read"; data: GovernanceDashboardReadV1 }>
  | Readonly<{ kind: "governance_read_refusal"; code: GovernanceReadRefusalCode; message: string }>;

// FEAT-068 V1 confirmed governance-action transport. The server validates the
// closed runtime variants; these types make caller-supplied authority impossible.
/**
 * Validate and detach the read transport before a browser renders it. The
 * server remains the policy authority; this boundary only refuses malformed
 * transport and preserves the server's already-deterministic queue order.
 */
export function projectGovernanceDashboardModel(input: unknown): GovernanceDashboardReadV1 | undefined {
  if (!isGovernanceObject(input, ["schemaVersion", "projectId", "remediations", "replans", "architectureDebt", "queue", "metrics", "rollout"]) || input.schemaVersion !== "hepha-governance-dashboard/v1" || !isGovernanceString(input.projectId) || !Array.isArray(input.remediations) || !Array.isArray(input.replans) || !Array.isArray(input.architectureDebt) || !Array.isArray(input.queue) || !isGovernanceMetrics(input.metrics) || !isGovernanceRollout(input.rollout)) return undefined;
  if (!input.remediations.every(isGovernanceRemediation) || !input.replans.every(isGovernanceReplan) || !input.architectureDebt.every(isGovernanceDebt) || !input.queue.every(isGovernanceQueueItem)) return undefined;
  return freezeGovernanceValue(JSON.parse(JSON.stringify(input)) as GovernanceDashboardReadV1);
}

type GovernanceRecord = Record<string, unknown>;
function isGovernanceRecord(value: unknown): value is GovernanceRecord { return typeof value === "object" && value !== null && !Array.isArray(value); }
function isGovernanceObject(value: unknown, keys: readonly string[]): value is GovernanceRecord { return isGovernanceRecord(value) && Object.keys(value).length === keys.length && keys.every((key) => Object.prototype.hasOwnProperty.call(value, key)); }
function isGovernanceString(value: unknown): value is string { return typeof value === "string" && value.length > 0; }
function isGovernanceInteger(value: unknown): value is number { return typeof value === "number" && Number.isSafeInteger(value) && value >= 0; }
function isGovernanceStringArray(value: unknown): boolean { return Array.isArray(value) && value.every(isGovernanceString); }
function isGovernanceLiteral(value: unknown, allowed: readonly string[]): boolean { return typeof value === "string" && allowed.includes(value); }
const governanceManifestResults = ["APPROVED", "NEEDS_CHANGES", "BLOCKED"] as const;
const governanceGateStates = ["APPROVED", "REJECTED", "BLOCKED", "PENDING"] as const;
const governanceDispositions = ["IN_SCOPE_BLOCKER", "SCOPE_EXPANSION", "ARCHITECTURE_DEBT", "OBSERVATION"] as const;
const governanceSeverities = ["blocker", "required", "note", "info"] as const;
const governanceReceiptKinds = ["remediation_item", "test"] as const;
const governanceReceiptOutcomes = ["VERIFIED", "FAILED", "NOT_VERIFIABLE", "PASSED", "NOT_RUN"] as const;
const governanceReplanStates = ["NORMAL_REMEDIATION", "REMEDIATION_REPLAN_REQUIRED", "REPLAN_PENDING_APPROVAL", "REPLAN_APPROVED", "REPLAN_REJECTED", "BOUNDED_REMEDIATION_DISPATCHED", "REVIEW_PENDING"] as const;
const governanceDebtStates = ["PENDING_TRIAGE", "CONFIRMED", "DEFERRED", "ACCEPTED_RISK", "PLANNED", "CLOSED", "REJECTED", "MERGED", "SUPERSEDED"] as const;
const governanceDebtActions = ["ACCEPT_RISK", "CLOSE", "CONFIRM", "DEFER", "MERGE", "PLAN_LINK", "REASSIGN", "REJECT", "SUPERSEDE"] as const;
const governanceReplanActions = ["APPROVE_REPLAN", "REJECT_REPLAN"] as const;
const governanceScopeActions = ["ACCEPT_SCOPE_EXPANSION", "REJECT_SCOPE_EXPANSION"] as const;
function isGovernanceCount(value: unknown): boolean { return isGovernanceObject(value, ["key", "count"]) && isGovernanceString(value.key) && isGovernanceInteger(value.count); }
function isGovernanceMetrics(value: unknown): boolean { const countFamilies = ["reviewResults", "gateStates", "cycleStates", "findingDispositions", "ruleReferences", "recoveryStopReasons", "replanStates", "debtStates", "debtPriorities", "scopeDecisionOutcomes", "replanDecisionOutcomes", "futureTouchDecisionKinds", "dispatchOutcomes", "shadowOutcomes", "pilotOutcomes"]; const totals = ["reviewRuns", "openRemediationCycles", "replanAggregates", "architectureDebtRecords", "actionableQueueItems", "postFixManifestations", "acceptedScopeExpansions"]; return isGovernanceObject(value, [...countFamilies, ...totals]) && countFamilies.every((key) => Array.isArray(value[key]) && value[key].every(isGovernanceCount)) && totals.every((key) => isGovernanceInteger(value[key])); }
const governanceParityCategories = ["SCHEMA", "PROJECT", "SOURCE_VERSION", "REPLAN", "DEBT", "QUEUE", "METRICS"] as const;
function isGovernanceHash(value: unknown): boolean { return typeof value === "string" && /^[a-f0-9]{64}$/.test(value); }
function isGovernanceRollout(value: unknown): boolean {
  if (!isGovernanceObject(value, ["mode", "eventVersion", "parity", "migration", "pilot"]) || !isGovernanceLiteral(value.mode, ["DISABLED", "ACTIVE", "NEEDS_HUMAN"]) || !isGovernanceInteger(value.eventVersion)) return false;
  const parity = value.parity;
  if (parity !== null && (!isGovernanceObject(parity, ["receiptId", "projectionSchema", "sourceVersionHash", "authoritativeHash", "dashboardHash", "result", "differenceCategories", "comparedAt", "validUntil"]) || !isGovernanceString(parity.receiptId) || parity.projectionSchema !== "hepha-governance-parity/v1" || ![parity.sourceVersionHash, parity.authoritativeHash, parity.dashboardHash].every(isGovernanceHash) || !isGovernanceLiteral(parity.result, ["MATCH", "MISMATCH"]) || !Array.isArray(parity.differenceCategories) || !parity.differenceCategories.every((category) => isGovernanceLiteral(category, governanceParityCategories)) || new Set(parity.differenceCategories).size !== parity.differenceCategories.length || !isGovernanceString(parity.comparedAt) || !isGovernanceString(parity.validUntil))) return false;
  const migration = value.migration;
  const validMigration = migration === null || (isGovernanceObject(migration, ["auditId", "schemaArea", "fromVersion", "toVersion", "outcome", "completedAt", "readBackHash"]) && isGovernanceString(migration.auditId) && migration.schemaArea === "GOVERNANCE_ROLLOUT" && isGovernanceInteger(migration.fromVersion) && isGovernanceInteger(migration.toVersion) && isGovernanceLiteral(migration.outcome, ["APPLIED", "ALREADY_CURRENT", "FAILED"]) && isGovernanceString(migration.completedAt) && (migration.readBackHash === null || isGovernanceHash(migration.readBackHash)));
  const pilot = value.pilot;
  const validPilot = pilot === null || (isGovernanceObject(pilot, ["pilotId", "featureId", "phaseContractId", "taskId", "contractVersion", "pilotConfigHash", "approvalReceiptId", "approvedAt", "expiresAt", "lastOutcome"]) && ["pilotId", "featureId", "phaseContractId", "taskId", "approvalReceiptId", "approvedAt", "expiresAt", "lastOutcome"].every((key) => isGovernanceString(pilot[key])) && isGovernanceInteger(pilot.contractVersion) && isGovernanceHash(pilot.pilotConfigHash));
  return validMigration && validPilot && ((value.mode === "DISABLED" && value.eventVersion === 0 && pilot === null) || (value.mode !== "DISABLED" && pilot !== null));
}
function isGovernanceRemediation(value: unknown): boolean { return isGovernanceObject(value, ["reviewRunId", "featureId", "phaseNumber", "reviewGateId", "manifestHash", "manifestResult", "ruleSnapshotHash", "createdAt", "gate", "cycleState", "findings", "receipts"]) && ["reviewRunId", "featureId", "reviewGateId", "manifestHash", "ruleSnapshotHash", "createdAt", "cycleState"].every((key) => isGovernanceString(value[key])) && isGovernanceLiteral(value.manifestResult, governanceManifestResults) && isGovernanceInteger(value.phaseNumber) && isGovernanceObject(value.gate, ["gateState", "reasonCode", "basisManifestHash", "cycleId", "decidedAt"]) && isGovernanceLiteral(value.gate.gateState, governanceGateStates) && isGovernanceString(value.gate.reasonCode) && isGovernanceString(value.gate.basisManifestHash) && (value.gate.cycleId === null || isGovernanceString(value.gate.cycleId)) && isGovernanceString(value.gate.decidedAt) && Array.isArray(value.findings) && value.findings.every((finding) => isGovernanceObject(finding, ["findingId", "findingObservationId", "disposition", "severity", "defectClass", "summary", "scopeDecisionTarget"]) && ["findingId", "findingObservationId", "defectClass", "summary"].every((key) => isGovernanceString(finding[key])) && isGovernanceLiteral(finding.disposition, governanceDispositions) && isGovernanceLiteral(finding.severity, governanceSeverities) && (finding.scopeDecisionTarget === null || (isGovernanceObject(finding.scopeDecisionTarget, ["aggregateId", "expectedVersion"]) && isGovernanceString(finding.scopeDecisionTarget.aggregateId) && isGovernanceInteger(finding.scopeDecisionTarget.expectedVersion)))) && Array.isArray(value.receipts) && value.receipts.every((receipt) => isGovernanceObject(receipt, ["findingId", "subjectKind", "subjectId", "outcome"]) && isGovernanceString(receipt.findingId) && isGovernanceLiteral(receipt.subjectKind, governanceReceiptKinds) && isGovernanceString(receipt.subjectId) && isGovernanceLiteral(receipt.outcome, governanceReceiptOutcomes)); }
function isGovernanceReplan(value: unknown): boolean { return isGovernanceObject(value, ["aggregateId", "featureId", "phaseNumber", "reviewGateId", "defectClass", "state", "eventVersion", "recurrence", "currentRequest", "scopeExpansionDecisions", "replanDecisions", "dispatch", "summary", "availableActions"]) && ["aggregateId", "featureId", "reviewGateId", "defectClass"].every((key) => isGovernanceString(value[key])) && isGovernanceLiteral(value.state, governanceReplanStates) && isGovernanceInteger(value.phaseNumber) && isGovernanceInteger(value.eventVersion) && isGovernanceObject(value.recurrence, ["postFixManifestations", "acceptedScopeExpansions"]) && Object.values(value.recurrence).every(isGovernanceInteger) && (value.currentRequest === null || (isGovernanceObject(value.currentRequest, ["requestId", "planHash", "planVersion", "requestedAt"]) && isGovernanceString(value.currentRequest.requestId) && isGovernanceString(value.currentRequest.planHash) && isGovernanceInteger(value.currentRequest.planVersion) && isGovernanceString(value.currentRequest.requestedAt))) && Array.isArray(value.scopeExpansionDecisions) && value.scopeExpansionDecisions.every((item) => isGovernanceObject(item, ["decisionId", "findingObservationId", "outcome", "actorId", "authorizedRole", "reason", "expectedVersion", "resultingVersion", "decidedAt"]) && ["decisionId", "findingObservationId", "actorId", "reason", "decidedAt"].every((key) => isGovernanceString(item[key])) && isGovernanceLiteral(item.outcome, ["ACCEPT", "REJECT"]) && item.authorizedRole === "FEATURE_OWNER" && isGovernanceInteger(item.expectedVersion) && isGovernanceInteger(item.resultingVersion)) && Array.isArray(value.replanDecisions) && value.replanDecisions.every((item) => isGovernanceObject(item, ["decisionId", "requestId", "planHash", "planVersion", "outcome", "actorId", "authorizedRole", "reason", "expectedVersion", "resultingVersion", "decidedAt"]) && ["decisionId", "requestId", "planHash", "actorId", "reason", "decidedAt"].every((key) => isGovernanceString(item[key])) && isGovernanceLiteral(item.outcome, ["APPROVE", "REJECT"]) && item.authorizedRole === "ARCHITECTURE_STEWARD" && ["planVersion", "expectedVersion", "resultingVersion"].every((key) => isGovernanceInteger(item[key]))) && (value.dispatch === null || (isGovernanceObject(value.dispatch, ["outcome", "workflowRunId", "attemptedAt"]) && isGovernanceLiteral(value.dispatch.outcome, ["STARTED", "START_FAILED"]) && isGovernanceString(value.dispatch.workflowRunId) && isGovernanceString(value.dispatch.attemptedAt))) && isGovernanceObject(value.summary, ["observations", "requests", "decisions", "dispatchAttempts", "reviewAssessments"]) && Object.values(value.summary).every(isGovernanceInteger) && Array.isArray(value.availableActions) && value.availableActions.every((action) => isGovernanceLiteral(action, governanceReplanActions)); }
function isGovernanceDebt(value: unknown): boolean { const required = ["recordId", "state", "eventVersion", "ownerId", "priority", "prioritySource", "rule", "architecturalBoundary", "rationale", "risk", "locations", "futureTouchTrigger", "discovery", "futureTouchDecisions", "availableActions"]; const optional = ["duplicateOfRecordId", "supersededByRecordId"]; return isGovernanceRecord(value) && Object.keys(value).every((key) => required.includes(key) || optional.includes(key)) && required.every((key) => Object.prototype.hasOwnProperty.call(value, key)) && optional.every((key) => value[key] === undefined || isGovernanceString(value[key])) && ["recordId", "ownerId", "architecturalBoundary", "rationale", "risk"].every((key) => isGovernanceString(value[key])) && isGovernanceLiteral(value.state, governanceDebtStates) && isGovernanceLiteral(value.priority, ["P0", "P1", "P2", "P3"]) && isGovernanceLiteral(value.prioritySource, ["AUTO_PENDING_DEFAULT", "STEWARD_CONFIRMED"]) && isGovernanceInteger(value.eventVersion) && isGovernanceObject(value.rule, ["ruleId", "ruleVersion", "category", "sourceReference"]) && Object.values(value.rule).every(isGovernanceString) && isGovernanceObject(value.futureTouchTrigger, ["triggerId", "name", "paths", "symbols", "ruleTags"]) && isGovernanceString((value.futureTouchTrigger as GovernanceRecord).triggerId) && isGovernanceString((value.futureTouchTrigger as GovernanceRecord).name) && ["paths", "symbols", "ruleTags"].every((key) => isGovernanceStringArray((value.futureTouchTrigger as GovernanceRecord)[key])) && isGovernanceObject(value.discovery, ["featureId", "phaseNumber", "reviewGateId", "findingId"]) && ["featureId", "reviewGateId", "findingId"].every((key) => isGovernanceString((value.discovery as GovernanceRecord)[key])) && isGovernanceInteger((value.discovery as GovernanceRecord).phaseNumber) && Array.isArray(value.locations) && value.locations.every((location) => isGovernanceRecord(location) && Object.keys(location).every((key) => ["locationId", "relativePath", "symbol", "endpoint", "ruleTags"].includes(key)) && isGovernanceString(location.locationId) && isGovernanceString(location.relativePath) && (location.symbol === undefined || isGovernanceString(location.symbol)) && (location.endpoint === undefined || isGovernanceString(location.endpoint)) && isGovernanceStringArray(location.ruleTags)) && Array.isArray(value.futureTouchDecisions) && value.futureTouchDecisions.every((decision) => isGovernanceObject(decision, ["decisionId", "featureId", "touchPlanHash", "recordVersion", "selectorIds", "kind", "actorId", "authorizedRole", "reason", "occurredAt"]) && ["decisionId", "featureId", "touchPlanHash", "actorId", "reason", "occurredAt"].every((key) => isGovernanceString(decision[key])) && isGovernanceInteger(decision.recordVersion) && isGovernanceStringArray(decision.selectorIds) && isGovernanceLiteral(decision.kind, ["REMEDIATE", "PREREQUISITE", "WAIVER", "NON_INTERACTION"]) && decision.authorizedRole === "ARCHITECTURE_STEWARD") && Array.isArray(value.availableActions) && value.availableActions.every((action) => isGovernanceLiteral(action, governanceDebtActions)); }
function isGovernanceQueueItem(value: unknown): boolean { if (!isGovernanceObject(value, ["itemId", "itemKind", "targetId", "featureId", "state", "currentVersion", "requiresAction", "urgency", "summaryCode", "availableActions"]) || !["itemId", "targetId", "featureId", "state", "summaryCode"].every((key) => isGovernanceString(value[key])) || !isGovernanceLiteral(value.itemKind, ["REMEDIATION", "REPLAN", "ARCHITECTURE_DEBT"]) || !isGovernanceLiteral(value.urgency, ["SCOPE_EXPANSION", "P0", "REPLAN_APPROVAL", "REPLAN_REQUIRED", "P1", "P2", "P3", "INFORMATIONAL"]) || (value.currentVersion !== null && !isGovernanceInteger(value.currentVersion)) || typeof value.requiresAction !== "boolean" || !Array.isArray(value.availableActions)) return false; const actions = value.itemKind === "REMEDIATION" ? governanceScopeActions : value.itemKind === "REPLAN" ? governanceReplanActions : governanceDebtActions; return value.availableActions.every((action) => isGovernanceLiteral(action, actions)); }
function freezeGovernanceValue<T>(value: T): T { if (value && typeof value === "object") { Object.values(value as Record<string, unknown>).forEach((child) => freezeGovernanceValue(child)); Object.freeze(value); } return value; }
